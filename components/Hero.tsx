// Hero renderiza apenas o fallback CSS.
// Peixes e bolhas são exibidos pelo OceanBackground global (fixed canvas).
import HeroFallback from './HeroFallback'

export default function Hero() {
  return <HeroFallback />
}
